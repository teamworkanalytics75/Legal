MODEL-002 + MODEL-003 — Calibration + Optuna HPO

Summary
- Add probability calibration (isotonic/sigmoid); report ECE/Brier
- Add Optuna HPO with pruning; log best params

Prerequisites
- Trained baseline models
- ML deps including Optuna

Action Box (copy/paste)
```
# 1) Environment
python -m venv .venv && source .venv/bin/activate
pip install -r requirements_upwork.txt
pip install -r Agents_1782_ML_Dataset/RequirementsMlSota.txt
pip install optuna

# 2) Run training with calibration
python Agents_1782_ML_Dataset/ml_system/pipelines/TrainingPipeline.py \
  --config configs/training_outline_bert.yaml \
  --calibration isotonic --metrics ece brier

# 3) Launch Optuna HPO (example; adjust n-trials)
python Agents_1782_ML_Dataset/ml_system/pipelines/TrainingPipeline.py \
  --config configs/training_outline_bert.yaml \
  --hpo optuna --n-trials 50 --pruning median
```

Validation
- Calibration: inspect ECE/Brier; verify thresholds per use-case
- HPO: confirm best params saved and training time bounded by pruning

Deliverables
- Updated training configs + model artifacts
- HPO summary in `Agents_1782_ML_Dataset/MLUPGRADEROADMAP.md`

Links
- Plan: MODEL-002, MODEL-003
- Roadmap: Agents_1782_ML_Dataset/MLUPGRADEROADMAP.md

